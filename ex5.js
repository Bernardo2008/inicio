//Calcular a média de três números e verificar se está aprovado ou reprovado:
//Escreva uma função que recebe três notas como parâmetro e calcula a média. 
//Em seguida, verifique se a média é maior ou igual a 7,0, indicando que o aluno foi aprovado, ou menor que 7,0, indicando que foi reprovado.

function VerificarAprovacao(nota1, nota2, nota3) {
    
    console.log(`${nota1}`)
    console.log(`${nota2}`)
    console.log(`${nota3}`)
    
    
    if (nota1 >= 7.0, nota2 >= 7.0, nota3 >= 7.0) {

        return "Aprovado";

    } else {

        return "Reprovado";

    }
}


console.log(VerificarAprovacao(8, 7, 9)); 

console.log(VerificarAprovacao(5, 4, 6)); 


